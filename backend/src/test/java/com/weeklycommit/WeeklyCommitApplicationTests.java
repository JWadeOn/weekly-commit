package com.weeklycommit;

import org.junit.jupiter.api.Test;

/**
 * Scaffold placeholder — no Spring context required here.
 * Feature-specific tests (CommitServiceTest, OAuthCallbackTest, etc.)
 * are added alongside their respective implementations.
 */
class WeeklyCommitApplicationTests {

    @Test
    void scaffoldPlaceholder() {
        // Intentionally empty — verifies compilation succeeds
    }
}
